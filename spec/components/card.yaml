component: card
description: Floating content container. Glass is optional — only use glass when the card sits in the floating layer, not embedded in content.
geometry:
  radius: 24
  padding: 24
  gap: 12
  minWidth: 240
typography:
  title: title3
  body: subheadline
